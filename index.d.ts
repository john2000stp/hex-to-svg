/**
 * Generates a 10x10 SVG with the specified hex color
 * @param hexColor - Hex color value (e.g., '#FF5733' or 'FF5733')
 * @returns SVG string
 */
declare function hexToSvg(hexColor: string): string;

export = hexToSvg;
